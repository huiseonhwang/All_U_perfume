<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.perfume.mybatis.Q_ABoardMapper">
	<select id = "getrecommendationList" resultType="com.perfume.beans.BoardDTO">
	<![CDATA[ 
		select * from tbl_board where boardnum = 1 order by b_number desc
	]]>
	</select>
	
	<!--  게시글 작성  -->
	<insert id="q_ainsert" parameterType="com.perfume.beans.BoardDTO">
		insert into tbl_board(boardnum,b_number,writer, password, subject, content) 
			values('1',SEQ_TBL_BOARD.nextval,#{writer, jdbcType=VARCHAR},#{password, jdbcType=VARCHAR},#{subject, jdbcType=VARCHAR},#{content, jdbcType=VARCHAR})
	</insert>
	
	<!--  게시글 출력 -->
	<select id="q_aContent" resultType="com.perfume.beans.BoardDTO">
		select * from tbl_board where b_number = #{b_number}
	</select>
	
	<!--  게시글 삭제 -->
	<delete id="q_adelete">
		delete tbl_board where b_number = #{b_number}
	</delete>
	
	<!--  게시글 수정  -->
	<update id="a_qnupdate">
		update tbl_board set subject=#{subject}, content=#{content}, reg_date = sysdate 
			where b_number = #{b_number}
	</update>
	
	<!-- 게시글 조회수 증가 -->
	<update id="q_areadcount">
		update tbl_board set readcount = readcount + 1 where b_number = #{b_number}
	</update>
	
<!-- 키워드 검색 -->
    <select id="selectQ_aBoard" resultType="com.perfume.beans.BoardDTO" parameterType="com.perfume.beans.SearchCriteria">
   	 <![CDATA[
    	select rm, b_number, subject, content, writer, reg_date, readcount, ref, re_step, re_level from
       		(select rownum rm, b_number, subject, content, writer, reg_date, readcount, ref, re_step, re_level from 
            	(select * from tbl_board where 1 = 1 and boardNum = 1]]>
             <include refid="search"></include> <![CDATA[ 
             	order by b_number desc)
       		 order by b_number desc) 
    	where rm between #{rowStart} and #{rowEnd} 
    ]]>
    </select>
	
	<select id="countQ_aBoard" parameterType="com.perfume.beans.SearchCriteria" resultType="int">
		select count (*) from tbl_board where boardnum = 1
			<include refid="search"></include>	
		   		AND b_number > 0
	</select>
	 
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 's'.toString()">AND subject LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'c'.toString()">AND CONTENT LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'w'.toString()">AND WRITER LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'sc'.toString()">AND (subject LIKE '%' || #{keyword} || '%') or (CONTENT LIKE '%' || #{keyword} || '%')</if>
		</if>
	</sql>
	
	<!-- 최상위 부모글 번호 생성 
	<select id = "o" resultType="int">
		select nvl(max(ref)+1,'1') as ref from tbl_board
	-->
	
	<!-- 같은 ref(최상위부모값)을 가진 것 중 현재 답글을 달 게시물의 순번보다 큰 것을 +1로 하여 밀리게 함
	<select id = "updateSetp" parameterType="com.perfume.beans.BoardDTO">
		update tbl_board set re_step = re_step + 1 where ref = # {ref} and re_step > #{re_step}
	</select>
	-->
	
	<!-- 현재 답글의 순번을 생성 
	<select id = "getStepMax" parameterType="com.perfume.beans.BoardDTO" resultType= "int">
		select decode(#{re_step),'0',(max(re_step)+1), #{re_step}+1) as re_step from tbl_board where ref=#{ref}
	</select>
	-->
	
	<!--  답글 입력 -->
	<insert id = "re_Q_a_Insert" parameterType = "com.perfume.beans.BoardDTO">
		insert into tbl_board(boardnum,b_number, ref, re_step, depth, writer, password, subject, content) 
			values('1',SEQ_TBL_BOARD.nextval, #{ref}, #{re_step}, #{depth}+1, #{writer, jdbcType=VARCHAR},
				#{password, jdbcType=VARCHAR},#{subject, jdbcType=VARCHAR},#{content, jdbcType=VARCHAR})
		<selectKey resultType="int" keyProperty="b_number" order="AFTER">
  			select max(b_number) as b_number FROM tbl_board
   		</selectKey>	
	</insert>
	
	
	
	<!--  @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ -->
	
		<select id="getArticles" resultType="com.perfume.beans.BoardDTO">
		<![CDATA[
			select * from (
				select num,writer,email,subject,passwd,reg_date,ref,re_step,re_level,content,ip,readcount,rownum r from (
					select * from board order by ref desc, re_step asc) 
				order by ref desc, re_step asc ) 
			where r >= #{startRow} and r <= #{endRow} 
		]]>
	</select>
	
	<select id="maxNum" resultType="int">
		select max(b_number) from tbl_board
	</select>
	
	<update id="stepUpdate">
		update tbl_board set re_step=re_step+1 where ref= #{ref} and re_step> #{re_step}
	</update>
	<insert id="insertArticle">
		insert into board(b_number,writer,subject,password,reg_date,ref,re_step,re_level,content) 
		values(SEQ_TBL_BOARD.NEXTVAL,
				#{writer},
				#{subject},
				#{password},
				#{reg_date},
				#{ref},
				#{re_step},
				#{re_level},
				#{content}
				)
	</insert>
</mapper>